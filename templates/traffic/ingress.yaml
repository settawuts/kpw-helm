apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "{{ .Values.kpwApp.name }}-ingress"
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /hello
spec:
  ingressClassName: public
  rules:

  # yourdomain.com
  - host: yourdomain.com
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.kpwApp.name }}
              port:
                number: {{ .Values.kpwApp.port }}

  # xx.yourdomain.com → /hello
  - host: xx.yourdomain.com
    http:
      paths:
        - path: /hello 
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.kpwApp.name }}
              port:
                number: {{ .Values.kpwApp.port }}

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-ingress
  namespace: argocd
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /argocd
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 80