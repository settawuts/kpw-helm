apiVersion: apps/v1
kind: Deployment
metadata:
  name: kpw-app
  namespace: kpw-app-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kpw-app
  template:
    metadata:
      labels:
        app: kpw-app
    spec:
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
          resources: 
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi