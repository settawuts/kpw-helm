apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.kpwApp.name }}-config"
data:
  default.conf: |
    server {
      listen 80;

      location / {
        root /usr/share/nginx/html;
        index index.html;
      }
    }